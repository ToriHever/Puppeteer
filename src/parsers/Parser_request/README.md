# Wordstat Parser v2.0

Улучшенная версия парсера Яндекс.Вордстат с модульной архитектурой, надёжной обработкой ошибок и системой восстановления после сбоев.

## 🎯 Основные улучшения

### Архитектура
- ✅ Модульная структура с разделением ответственности
- ✅ Объектно-ориентированный подход
- ✅ Конфигурация вынесена в отдельный файл
- ✅ Удобная структура проекта

### Обработка ошибок
- ✅ Retry механизм с экспоненциальной задержкой
- ✅ Детальное логирование с уровнями (INFO, WARN, ERROR, SUCCESS, DEBUG)
- ✅ Graceful shutdown при SIGINT/SIGTERM
- ✅ Обработка необработанных исключений

### Производительность
- ✅ Batch-обработка с периодическим сохранением состояния
- ✅ Прогресс-бар в логах
- ✅ Пропуск уже обработанных запросов при перезапуске
- ✅ Оптимизированные задержки

### Безопасность
- ✅ Credentials в переменных окружения (.env)
- ✅ Пример .env.example для безопасного шаринга
- ✅ .gitignore для исключения чувствительных данных

### Функциональность
- ✅ Автоматическое сохранение состояния при ошибках
- ✅ Возможность паузы/возобновления через UI в браузере
- ✅ Восстановление парсинга с места остановки
- ✅ UI панель управления в браузере
- ✅ Скрипт очистки состояния npm run clear-state
- ✅ Скрипт очистки результатов npm run clear-results

## 📁 Структура проекта

```
Parser_request/
├── index.js                  # Точка входа
├── config.js                 # Конфигурация
├── logger.js                 # Система логирования
├── utils.js                  # Вспомогательные функции
├── stateManager.js           # Управление состоянием
├── authManager.js            # Авторизация
├── WordstatParser.js         # Основной класс парсера
├── .env                      # Переменные окружения (не в git!)
├── .env.example              # Пример .env
├── package.json              # Зависимости
├── requests.txt              # Список запросов
└── Results/
    ├── WordStat.csv          # Результаты парсинга
    ├── parser_state.json     # Состояние для восстановления
    └── parser.log            # Логи выполнения
```

## 🚀 Установка

1. Клонируйте или скопируйте файлы проекта

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` своими данными:
```env
YANDEX_LOGIN=your_email@yandex.ru
YANDEX_PASSWORD=your_password
```

5. Добавьте запросы в `requests.txt` (один запрос на строку)

## 💻 Использование

### Запуск парсинга
```bash
npm start
```

### Управление в процессе работы

В окне браузера появится панель управления с кнопками:
- **⏸ Pause** - приостановить парсинг
- **▶️ Resume** - возобновить парсинг

При остановке через `Ctrl+C` состояние сохраняется автоматически.

### Возобновление после сбоя

Просто запустите скрипт снова:
```bash
npm start
```

Парсер автоматически продолжит с места остановки, используя сохранённое состояние.

### Очистка состояния

Если нужно начать парсинг заново:
```bash
npm run clear-state
```

### Очистка результатов

Если нужно удалить файл с результатами:
```bash
npm run clear-results
```

## 📊 Формат результатов

Результаты сохраняются в CSV файл `Results/WordStat.csv`:

| Запрос | Частота | Частота с кавычками | Частота с восклицаниями |
|--------|---------|---------------------|-------------------------|
| купить айфон | 12345 | 6789 | 3456 |

Для каждого запроса парсер собирает 3 метрики:
1. Обычная частота
2. Частота с кавычками `"запрос"`
3. Частота с восклицаниями `"!каждое !слово"`

## 🔧 Конфигурация

Все настройки находятся в `config.js`:

- **Таймауты**: навигация, селекторы, задержки
- **Retry**: количество попыток, задержки
- **Batch**: размер пакета, интервал сохранения
- **Селекторы**: CSS-селекторы элементов страницы

## 📝 Логирование

Логи записываются:
1. В консоль (с цветовой подсветкой)
2. В файл `Results/parser.log`

Уровни логирования:
- 🔵 **INFO** - информационные сообщения
- 🟡 **WARN** - предупреждения
- 🔴 **ERROR** - ошибки
- 🟢 **SUCCESS** - успешные операции
- ⚫ **DEBUG** - отладочная информация

## 🛡️ Обработка ошибок

Парсер обрабатывает следующие ситуации:

1. **Проблемы с авторизацией** - автоматическая повторная авторизация
2. **Сетевые ошибки** - retry с экспоненциальной задержкой
3. **Отсутствие результатов** - запись "0" в результаты
4. **Разрушение контекста** - пауза и ожидание действий пользователя
5. **Неожиданное завершение** - автосохранение состояния

## 🔐 Безопасность

**⚠️ ВАЖНО:**

1. Никогда не коммитьте файл `.env` в git
2. Используйте `.env.example` для документирования переменных
3. Добавьте `.env` в `.gitignore`
4. Не храните credentials в коде

## 📈 Мониторинг прогресса

Во время работы вы увидите:

```
[████████████████░░░░░░░░░░░░░░] 543/1000 (54.3%)
✓ купить телефон | original = 15234
✓ купить телефон | withQuotes = 8456
✓ купить телефон | withExclamation = 4123
✓ Запрос завершен: купить телефон
```

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте файл логов `Results/parser.log`
2. Убедитесь, что `.env` правильно заполнен
3. Проверьте доступность Яндекс.Вордстат

## 📄 Лицензия

MIT