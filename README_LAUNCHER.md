# 🚀 Parser Launcher

Единый интерфейс для запуска всех парсеров проекта.

## 📋 Возможности

- 🎨 Красивый терминальный интерфейс с цветным оформлением
- 🔍 Автоматическая проверка доступности парсеров
- ⚡ Запуск любого парсера в 1 клик
- 🛡️ Обработка ошибок и корректное завершение
- 📊 Поддержка всех парсеров проекта:
  - Wordstat Parser (Yandex Wordstat)
  - Multi Search - Yandex
  - Multi Search - Google
  - Multi Search - Both (Sequential/Parallel)

## 🚀 Быстрый старт

### 1. Установка

Сохраните файлы в корневую директорию проекта (там где находятся папки `Parser_request` и `multi_search_parser`):

```
Puppeteer/
├── launcher.js          ← Главный файл launcher
├── package.json         ← Конфигурация (создайте новый)
├── Parser_request/
│   └── index.js
└── multi_search_parser/
    └── index.js
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск

```bash
npm start
```

Или напрямую:

```bash
node launcher.js
```

## 📖 Использование

1. **Запустите launcher**
   ```bash
   npm start
   ```

2. **Выберите парсер**
   - Введите номер парсера (1-5)
   - Или 0 для выхода

3. **Отредактируйте файл с запросами**
   - Launcher автоматически откроет файл с запросами (requests.txt или queries.txt)
   - Для Windows: откроется Notepad
   - Для Mac: откроется TextEdit
   - Для Linux: откроется стандартный редактор
   - **Важно:** Закройте редактор после редактирования (для Windows)
   - Каждый запрос должен быть на новой строке

4. **Подтвердите запуск**
   - Введите `yes` для запуска
   - Любой другой ввод отменит запуск

5. **Парсер запустится**
   - Следуйте инструкциям конкретного парсера
   - Нажмите Ctrl+C для остановки

6. **Возврат в меню**
   - После завершения нажмите Enter
   - Выберите другой парсер или выйдите

## 🎯 Доступные парсеры

### 1️⃣ Wordstat Parser
- **Описание**: Парсинг частоты запросов из Яндекс.Вордстат
- **Файл**: `Parser_request/index.js`
- **Файл с запросами**: `Parser_request/requests.txt`
- **Требования**: 
  - Файл `requests.txt` с запросами (открывается автоматически)
  - Файл `.env` с учетными данными Яндекса

### 2️⃣ Multi Search - Yandex
- **Описание**: Парсинг результатов поиска Яндекс
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

### 3️⃣ Multi Search - Google
- **Описание**: Парсинг результатов поиска Google
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

### 4️⃣ Multi Search - Both (Sequential)
- **Описание**: Парсинг обоих поисковиков последовательно
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

### 5️⃣ Multi Search - Both (Parallel)
- **Описание**: Парсинг обоих поисковиков параллельно
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

## ⚙️ Конфигурация

Для добавления нового парсера отредактируйте объект `PARSERS` в `launcher.js`:

```javascript
const PARSERS = {
  'your-parser': {
    name: 'Your Parser Name',
    description: 'Описание парсера',
    path: './path/to/parser.js',
    icon: '🔧',
    color: 'green',
    args: ['--optional', 'args']  // необязательно
  }
};
```

### Доступные цвета

- `cyan` (голубой)
- `yellow` (желтый)
- `blue` (синий)
- `magenta` (пурпурный)
- `green` (зеленый)
- `red` (красный)
- `white` (белый)
- `gray` (серый)

## 🛠️ Устранение неполадок

### Ошибка "Cannot find module 'chalk'"

```bash
npm install chalk
```

### Ошибка "Файл парсера не найден"

1. Проверьте, что `launcher.js` находится в корне проекта
2. Проверьте пути к парсерам в конфигурации
3. Убедитесь, что файлы парсеров существуют

### Парсер не запускается

1. Проверьте, что все зависимости установлены в папках парсеров
2. Убедитесь, что файлы конфигурации (`.env`, `queries.txt` и т.д.) созданы
3. Запустите парсер напрямую для диагностики:
   ```bash
   cd Parser_request
   npm start
   ```

## 📝 Горячие клавиши

- **Ctrl+C** - Выход из launcher
- **0** - Выход из меню
- **1-5** - Выбор парсера

## 🔧 Разработка

### Структура launcher

```javascript
class ParserLauncher {
  clearScreen()        // Очистка экрана
  drawHeader()         // Отрисовка заголовка
  drawMenu()           // Отрисовка меню
  drawParserInfo()     // Информация о парсере
  runParser()          // Запуск парсера
  prompt()             // Ввод пользователя
  pause()              // Пауза
  mainMenu()           // Главное меню
  start()              // Старт launcher
}
```

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте все зависимости
2. Убедитесь в правильности путей
3. Проверьте логи парсеров