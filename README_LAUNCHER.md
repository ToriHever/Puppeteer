# 🚀 Parser Launcher

Единый интерфейс для запуска всех парсеров проекта.

## 📋 Возможности

- 🎨 Красивый терминальный интерфейс с цветным оформлением
- 🔍 Автоматическая проверка доступности парсеров
- ⚡ Запуск любого парсера в 1 клик
- 🛡️ Обработка ошибок и корректное завершение
- 📊 Поддержка всех парсеров проекта:
  - Wordstat Parser (Yandex Wordstat)
  - Multi Search - Yandex
  - Multi Search - Google
  - Multi Search - Both (Sequential/Parallel)

## 🚀 Быстрый старт

### 1. Установка

Сохраните файлы в корневую директорию проекта (там где находятся папки `Parser_request` и `multi_search_parser`):

```
Puppeteer/
├── launcher.js          ← Главный файл launcher
├── package.json         ← Конфигурация (создайте новый)
├── Parser_request/
│   └── index.js
└── multi_search_parser/
    └── index.js
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск

```bash
npm start
```

Или напрямую:

```bash
node launcher.js
```

## 📖 Использование

1. **Запустите launcher**
   ```bash
   npm start
   ```

2. **Выберите парсер**
   - Введите номер парсера (1-5)
   - Или 0 для выхода

3. **Отредактируйте файл с запросами**
   - Launcher автоматически откроет файл с запросами (requests.txt или queries.txt)
   - Для Windows: откроется Notepad
   - Для Mac: откроется TextEdit
   - Для Linux: откроется стандартный редактор
   - **Важно:** Закройте редактор после редактирования (для Windows)
   - Каждый запрос должен быть на новой строке

4. **Подтвердите запуск**
   - Введите `yes` для запуска
   - Любой другой ввод отменит запуск

5. **Парсер запустится**
   - Следуйте инструкциям конкретного парсера
   - Нажмите Ctrl+C для остановки

6. **Возврат в меню**
   - После завершения нажмите Enter
   - Выберите другой парсер или выйдите

## 🎯 Доступные парсеры

### 1️⃣ Wordstat Parser
- **Описание**: Парсинг частоты запросов из Яндекс.Вордстат
- **Файл**: `Parser_request/index.js`
- **Файл с запросами**: `Parser_request/requests.txt`
- **Требования**: 
  - Файл `requests.txt` с запросами (открывается автоматически)
  - Файл `.env` с учетными данными Яндекса

### 2️⃣ Wordstat API Parser
- **Описание**: Получение динамики запросов через Wordstat API
- **Файл**: `Parser_wordstat_api/wordstat-api.js`
- **Файл с запросами**: `Parser_wordstat_api/requests.txt`
- **Требования**: 
  - Файл `requests.txt` с запросами (открывается автоматически)
  - Файл `.env` с `WORDSTAT_API_TOKEN`
  - Токен API от Яндекс.Вордстат

### 3️⃣ Multi Search - Yandex
- **Описание**: Парсинг результатов поиска Яндекс
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

### 4️⃣ Multi Search - Google
- **Описание**: Парсинг результатов поиска Google
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

### 5️⃣ Multi Search - Both (Sequential)
- **Описание**: Парсинг обоих поисковиков последовательно
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

### 6️⃣ Multi Search - Both (Parallel)
- **Описание**: Парсинг обоих поисковиков параллельно
- **Файл**: `multi_search_parser/index.js`
- **Файл с запросами**: `multi_search_parser/scripts/queries.txt`
- **Требования**: Файл `queries.txt` с запросами (открывается автоматически)

## ⚙️ Конфигурация

Для добавления нового парсера отредактируйте объект `PARSERS` в `launcher.js`:

```javascript
const PARSERS = {
  'your-parser': {
    name: 'Your Parser Name',
    description: 'Описание парсера',
    path: './path/to/parser.js',
    icon: '🔧',
    color: 'green',
    args: ['--optional', 'args']  // необязательно
  }
};
```

### Настройка Wordstat API Parser

Для работы Wordstat API парсера нужно:

1. **Получить API токен:**
   - Перейдите на https://oauth.yandex.ru/
   - Зарегистрируйте приложение
   - Получите токен доступа

2. **Добавить токен в `.env`:**
   ```env
   # В корневом файле .env
   WORDSTAT_API_TOKEN=ваш_api_токен_здесь
   ```

3. **Создать файл с запросами:**
   ```
   Parser_wordstat_api/requests.txt
   ```

4. **Установить зависимости для парсера:**
   ```bash
   cd Parser_wordstat_api
   npm install
   cd ..
   ```

### Доступные цвета

- `cyan` (голубой)
- `yellow` (желтый)
- `blue` (синий)
- `magenta` (пурпурный)
- `green` (зеленый)
- `red` (красный)
- `white` (белый)
- `gray` (серый)

## 🛠️ Устранение неполадок

### Ошибка "Cannot find module 'chalk'"

```bash
npm install chalk
```

### Ошибка "Файл парсера не найден"

1. Проверьте, что `launcher.js` находится в корне проекта
2. Проверьте пути к парсерам в конфигурации
3. Убедитесь, что файлы парсеров существуют

### Парсер не запускается

1. Проверьте, что все зависимости установлены в папках парсеров
2. Убедитесь, что файлы конфигурации (`.env`, `queries.txt` и т.д.) созданы
3. Запустите парсер напрямую для диагностики:
   ```bash
   cd Parser_request
   npm start
   ```

## 📝 Горячие клавиши

- **Ctrl+C** - Выход из launcher
- **0** - Выход из меню
- **1-5** - Выбор парсера

## 🔧 Разработка

### Структура launcher

```javascript
class ParserLauncher {
  clearScreen()        // Очистка экрана
  drawHeader()         // Отрисовка заголовка
  drawMenu()           // Отрисовка меню
  drawParserInfo()     // Информация о парсере
  runParser()          // Запуск парсера
  prompt()             // Ввод пользователя
  pause()              // Пауза
  mainMenu()           // Главное меню
  start()              // Старт launcher
}
```

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте все зависимости
2. Убедитесь в правильности путей
3. Проверьте логи парсеров

# 📝 Как добавить новый парсер в launcher

## 🚀 Быстрая инструкция (3 шага)

### Шаг 1: Откройте `launcher.js`

Найдите объект `PARSERS` в начале файла (примерно строка 17).

### Шаг 2: Добавьте новый парсер

Добавьте новую запись в объект `PARSERS`:

```javascript
const PARSERS = {
  // ... существующие парсеры ...
  
  'ваш-ключ': {
    name: 'Название вашего парсера',
    description: 'Краткое описание что делает парсер',
    path: './путь/к/парсеру/index.js',
    icon: '🔧', // Выберите иконку
    color: 'green' // Выберите цвет
  }
};
```

### Шаг 3: (Опционально) Добавьте путь к файлу запросов

Если ваш парсер использует файл с запросами (как `requests.txt` или `queries.txt`), добавьте путь в метод `openRequestsFile`:

```javascript
async openRequestsFile(parserKey) {
  const requestsFiles = {
    // ... существующие файлы ...
    
    'ваш-ключ': path.join(__dirname, 'Ваша_папка', 'requests.txt')
  };
  // ...
}
```

---

## 📋 Полный пример: Добавление Wordstat API парсера

### 1. Добавляем в PARSERS:

```javascript
const PARSERS = {
  // Существующие парсеры...
  
  'wordstat-api': {
    name: 'Wordstat API Parser',
    description: 'Получение динамики запросов через Wordstat API',
    path: './Parser_wordstat_api/wordstat-api.js',
    icon: '📈',
    color: 'green'
  }
};
```

### 2. Добавляем путь к файлу запросов:

```javascript
async openRequestsFile(parserKey) {
  const requestsFiles = {
    'wordstat': path.join(__dirname, 'Parser_request', 'requests.txt'),
    'wordstat-api': path.join(__dirname, 'Parser_wordstat_api', 'requests.txt'), // ← Новая строка
    'multi-yandex': path.join(__dirname, 'multi_search_parser', 'scripts', 'queries.txt'),
    // ...
  };
  // ...
}
```

### 3. Готово! ✅

Теперь парсер появится в меню под номером 2.

---

## 🎨 Доступные цвета

```javascript
color: 'cyan'      // 🔵 Голубой
color: 'yellow'    // 🟡 Желтый
color: 'blue'      // 🔵 Синий
color: 'magenta'   // 🟣 Пурпурный
color: 'green'     // 🟢 Зеленый
color: 'red'       // 🔴 Красный
color: 'white'     // ⚪ Белый
color: 'gray'      // ⚫ Серый
```

---

## 🎯 Популярные иконки для парсеров

```
📊 - Статистика, графики
📈 - Рост, тренды, динамика
📉 - Аналитика
🔍 - Поиск
🔎 - Детальный поиск
⚡ - Быстрая обработка
🔄 - Циклическая обработка
🔧 - Настройки, утилиты
📝 - Текстовые данные
🌐 - Веб-парсинг
🤖 - Автоматизация
💾 - Сохранение данных
```

---

## 🛠️ Дополнительные параметры

### Передача аргументов парсеру

Если ваш парсер принимает аргументы командной строки:

```javascript
'ваш-парсер': {
  name: 'Название',
  description: 'Описание',
  path: './путь/parser.js',
  icon: '🔧',
  color: 'blue',
  args: ['--mode', 'fast', '--output', 'csv'] // ← Аргументы
}
```

Парсер будет запущен как:
```bash
node ./путь/parser.js --mode fast --output csv
```

---

## ✅ Чек-лист перед добавлением парсера

- [ ] Создана папка парсера с файлом `index.js` (или другим главным файлом)
- [ ] Установлены зависимости в папке парсера (`cd папка && npm install`)
- [ ] Создан файл с запросами (если нужен)
- [ ] Создан `.env` с нужными переменными (если нужен)
- [ ] Парсер работает при прямом запуске (`node папка/index.js`)
- [ ] Добавлена запись в объект `PARSERS`
- [ ] Добавлен путь к файлу запросов (если есть)
- [ ] Протестирован запуск через launcher

---

## 🔍 Структура проекта с новым парсером

```
Puppeteer/
├── launcher.js                    ← Главный launcher
├── Parser_request/                ← Парсер 1
│   ├── index.js
│   └── requests.txt
├── Parser_wordstat_api/           ← Парсер 2 (новый)
│   ├── wordstat-api.js
│   ├── requests.txt
│   └── package.json
├── multi_search_parser/           ← Парсер 3
│   ├── index.js
│   └── scripts/queries.txt
└── Ваш_новый_парсер/              ← Парсер 4 (добавляете)
    ├── index.js
    ├── config.txt
    └── package.json
```

---

## 💡 Советы

1. **Используйте понятные ключи:** `'wordstat-api'` вместо `'parser2'`
2. **Описание должно быть кратким:** Умещается в 1 строку
3. **Цвета не должны повторяться:** Для легкого различения
4. **Иконки помогают:** Визуально понятнее что делает парсер
5. **Путь должен быть относительным:** `./папка/файл.js`

---

## 🐛 Частые ошибки

### Ошибка: "Файл парсера не найден"

**Причина:** Неправильный путь в параметре `path`

**Решение:** Проверьте путь. Он должен быть относительно корня проекта:
```javascript
path: './Parser_wordstat_api/wordstat-api.js' // ✅ Правильно
path: 'Parser_wordstat_api/wordstat-api.js'   // ❌ Без ./
path: '/Parser_wordstat_api/wordstat-api.js'  // ❌ Абсолютный
```

### Ошибка: "Cannot find module"

**Причина:** Не установлены зависимости для парсера

**Решение:**
```bash
cd Parser_wordstat_api
npm install
```

### Парсер не появляется в меню

**Причина:** Забыли перезапустить launcher после изменений

**Решение:** Закройте launcher (Ctrl+C) и запустите снова

---

## 📞 Нужна помощь?

Если возникли проблемы при добавлении парсера:
1. Проверьте синтаксис в `launcher.js` (не